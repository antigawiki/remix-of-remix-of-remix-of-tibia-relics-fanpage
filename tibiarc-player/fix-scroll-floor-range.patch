--- a/lib/parser.hpp
+++ b/lib/parser.hpp
@@ -89,7 +89,8 @@ private:
     void ParseMapDescription(DataReader &reader,
                              EventList &events,
                              int xOffset,
                              int yOffset,
                              int width,
-                             int height);
+                             int height,
+                             bool isScroll = false);
 
     void ParseAmbientLight(DataReader &reader, EventList &events);
--- a/lib/parser.cpp
+++ b/lib/parser.cpp
@@ -417,18 +417,26 @@ void Parser::ParseMapDescription(DataReader &reader,
                                  EventList &events,
                                  int xOffset,
                                  int yOffset,
                                  int width,
-                                 int height) {
+                                 int height,
+                                 bool isScroll) {
     int zIdx, endZ, zStep;
     uint16_t tileSkip;
 
     if (Position_.Z > 7) {
         zIdx = Position_.Z - 2;
         endZ = std::min<int>(15, Position_.Z + 2);
         zStep = 1;
-    } else {
+    } else if (isScroll) {
+        /* TibiaRelic recordings send exactly 1 floor per scroll update,
+         * not the full 7→0 range. Reading more than 1 floor causes the
+         * parser to consume subsequent opcodes as tile data, producing
+         * ghost creatures and corrupted game state. */
+        zIdx = Position_.Z;
+        endZ = Position_.Z;
+        zStep = -1;
+    } else {
         zIdx = 7;
         endZ = 0;
         zStep = -1;
@@ -945,7 +953,7 @@ void Parser::ParseMoveEast(DataReader &reader, EventList &events) {
     auto &event = AddEvent<PlayerMoved>(events);
     event.Position = Position_;
 
-    ParseMapDescription(reader, events, +9, -6, 1, Map::TileBufferHeight);
+    ParseMapDescription(reader, events, +9, -6, 1, Map::TileBufferHeight, true);
 }
 
 void Parser::ParseMoveNorth(DataReader &reader, EventList &events) {
@@ -955,7 +963,7 @@ void Parser::ParseMoveNorth(DataReader &reader, EventList &events) {
     auto &event = AddEvent<PlayerMoved>(events);
     event.Position = Position_;
 
-    ParseMapDescription(reader, events, -8, -6, Map::TileBufferWidth, 1);
+    ParseMapDescription(reader, events, -8, -6, Map::TileBufferWidth, 1, true);
 }
 
 void Parser::ParseMoveSouth(DataReader &reader, EventList &events) {
@@ -966,7 +974,7 @@ void Parser::ParseMoveSouth(DataReader &reader, EventList &events) {
     auto &event = AddEvent<PlayerMoved>(events);
     event.Position = Position_;
 
-    ParseMapDescription(reader, events, -8, +7, Map::TileBufferWidth, 1);
+    ParseMapDescription(reader, events, -8, +7, Map::TileBufferWidth, 1, true);
 }
 
 void Parser::ParseMoveWest(DataReader &reader, EventList &events) {
@@ -977,7 +985,7 @@ void Parser::ParseMoveWest(DataReader &reader, EventList &events) {
     auto &event = AddEvent<PlayerMoved>(events);
     event.Position = Position_;
 
-    ParseMapDescription(reader, events, -8, -6, 1, Map::TileBufferHeight);
+    ParseMapDescription(reader, events, -8, -6, 1, Map::TileBufferHeight, true);
 }
 
 Parser::EventList Parser::Parse(DataReader &reader) {
