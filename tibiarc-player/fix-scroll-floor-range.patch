--- a/lib/parser.cpp
+++ b/lib/parser.cpp
@@ -1580,17 +1580,15 @@ void Parser::ParseFloorChangeUp(DataReader &reader, EventList &events) {
     Position_.Z--;
     tileSkip = 0;
 
     if (Position_.Z == 7) {
-        for (int zIdx = 5; zIdx >= 0; zIdx--) {
-            tileSkip = ParseFloorDescription(reader,
-                                             events,
-                                             Position_.X - 8,
-                                             Position_.Y - 6,
-                                             zIdx,
-                                             Map::TileBufferWidth,
-                                             Map::TileBufferHeight,
-                                             Map::TileBufferDepth - zIdx,
-                                             tileSkip);
-        }
+        /* TibiaRelic: only the newly visible floor (z=5) is sent when
+         * transitioning from underground to surface, not all 6 floors. */
+        tileSkip = ParseFloorDescription(reader,
+                                         events,
+                                         Position_.X - 8,
+                                         Position_.Y - 6,
+                                         5,
+                                         Map::TileBufferWidth,
+                                         Map::TileBufferHeight,
+                                         3,
+                                         tileSkip);
     } else if (Position_.Z > 7) {
         tileSkip = ParseFloorDescription(reader,
                                          events,
@@ -1940,7 +1938,8 @@ void Parser::ParseMoveEast(DataReader &reader, EventList &events) {
     auto &event = AddEvent<PlayerMoved>(events);
     event.Position = Position_;
 
-    ParseMapDescription(reader, events, +9, -6, 1, Map::TileBufferHeight);
+    /* TibiaRelic: full 18x14 viewport sent on scroll, not just new column */
+    ParseMapDescription(reader, events, -8, -6, Map::TileBufferWidth, Map::TileBufferHeight);
 }
 
 void Parser::ParseMoveNorth(DataReader &reader, EventList &events) {
@@ -1951,7 +1950,8 @@ void Parser::ParseMoveNorth(DataReader &reader, EventList &events) {
     auto &event = AddEvent<PlayerMoved>(events);
     event.Position = Position_;
 
-    ParseMapDescription(reader, events, -8, -6, Map::TileBufferWidth, 1);
+    /* TibiaRelic: full 18x14 viewport sent on scroll, not just new row */
+    ParseMapDescription(reader, events, -8, -6, Map::TileBufferWidth, Map::TileBufferHeight);
 }
 
 void Parser::ParseMoveSouth(DataReader &reader, EventList &events) {
@@ -1963,7 +1963,8 @@ void Parser::ParseMoveSouth(DataReader &reader, EventList &events) {
     auto &event = AddEvent<PlayerMoved>(events);
     event.Position = Position_;
 
-    ParseMapDescription(reader, events, -8, +7, Map::TileBufferWidth, 1);
+    /* TibiaRelic: full 18x14 viewport sent on scroll, not just new row */
+    ParseMapDescription(reader, events, -8, -6, Map::TileBufferWidth, Map::TileBufferHeight);
 }
 
 void Parser::ParseMoveWest(DataReader &reader, EventList &events) {
@@ -1975,5 +1976,6 @@ void Parser::ParseMoveWest(DataReader &reader, EventList &events) {
     auto &event = AddEvent<PlayerMoved>(events);
     event.Position = Position_;
 
-    ParseMapDescription(reader, events, -8, -6, 1, Map::TileBufferHeight);
+    /* TibiaRelic: full 18x14 viewport sent on scroll, not just new column */
+    ParseMapDescription(reader, events, -8, -6, Map::TileBufferWidth, Map::TileBufferHeight);
 }
